powerprompt
===========

A powerfull bash prompt with the ‘less is more principle’ for colors. If nothing special is happening, prompt will be all uncolored and can be configured to look exactly like the OS' default prompt.

Configuration
-------------

See the comments at the start of the script for more information on configuration. Example configuration file can be found as powerprompt.conf
